<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wcsb.wbs.mapper.DailyListEntryMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.wcsb.wbs.domain.DailyListEntry">
        <id column="id" property="id" />
        <result column="currentDate" property="currentDate" />
        <result column="entryToday" property="entryToday" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, currentDate, entryToday
    </sql>

</mapper>
